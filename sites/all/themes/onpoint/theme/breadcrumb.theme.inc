<?php 

/*
function datamentors_breadcrumb(&$variables) {
  // Returns default Drupal breadcrumbs
    // return theme_breadcrumb($variables);
  $breadcrumb = NULL;
}
*/

function datamentors_breadcrumb(&$variables) {
  return theme_breadcrumb($variables);
}
